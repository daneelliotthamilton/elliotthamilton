<div class="page-header">
  <h1><%= @service.title %></h1>
</div>

<div class="page-description">
  <%= @service.content %>
</div>
<div>
  <%= link_to "Edit this service", edit_admin_practice_area_service_path(@practice_area,@service) %> |
  <%= link_to "Back to #{@service.practice_area.title}", admin_practice_area_path(@service.practice_area) %>

  <%= link_to "Destroy this service", admin_practice_area_service_path(@practice_area, @service), method: :delete, class: "btn btn-danger"  %>
</div>
